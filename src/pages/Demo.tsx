import React from 'react';
import { Box, Typography } from '@forge/foundry';

/** A demo space for you to play around in. */
const Demo: React.FC = () => {
  return (
    <Box m={5}>
      <Typography variant="h1" gutterBottom>
        Demo
      </Typography>
    </Box>
  );
};

export default Demo;
